#include <stdio.h>

int main (){

    int s;
    int age;

    printf("Nhap ten: ");
    scanf("%d", &s);

    printf("Tuoi: ");
    scanf("%d", &age);

    printf("Ten ban la %d, ban %d tuoi", s, age);

    return 0;
}